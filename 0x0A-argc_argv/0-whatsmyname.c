#include <stdio.h>
/**
 * main - passes argc and argv
 * @argc: argument count
 * @argv: aegument vector
 * Return: returns 0
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf(" %s\n", argv[0]);
	return (0);
}
